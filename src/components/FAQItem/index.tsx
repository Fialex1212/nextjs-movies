import React from 'react'

type FAQItemProps = {
    id: string;
    title: string;
    description: string;
}

const FAQItem: React.FC<FAQItemProps> = ({id, title, description}) => {
  return (
    <div>
      
    </div>
  )
}

export default FAQItem
